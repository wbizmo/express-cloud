<x-layout.app title="Lisa Conversation"><x-layout.app-shell page-title="{{ $conversation->title }}" page-description="Retained Lisa activity"><div class="space-y-4">@foreach($conversation->messages as $m)<article class="rounded-xl p-4 {{ $m->role==='user'?'bg-slate-950 text-white':'bg-slate-100' }}"><b class="text-xs uppercase">{{ $m->role }}</b><p class="mt-2 whitespace-pre-wrap">{{ $m->content }}</p><small class="mt-2 block opacity-60">{{ $m->created_at?->toDayDateTimeString() }}</small></article>@endforeach</div></x-layout.app-shell></x-layout.app>
