<x-layout.app title="Lisa Chat Audit"><x-layout.app-shell page-title="Lisa Chat Audit" page-description="Administrator-only retained chat history for every user."><div class="overflow-hidden rounded-2xl border bg-white"><table class="w-full text-left text-sm"><thead class="bg-slate-50"><tr><th class="p-3">Staff</th><th class="p-3">Branch</th><th class="p-3">Conversation</th><th class="p-3">Messages</th><th class="p-3">Last activity</th></tr></thead><tbody>@foreach($conversations as $c)<tr class="border-t"><td class="p-3">{{ trim(($c->account?->first_name??'').' '.($c->account?->last_name??'')) }}</td><td class="p-3">{{ $c->branch?->name ?? 'All/unspecified' }}</td><td class="p-3"><a class="font-semibold text-blue-700" href="{{ route('admin.insights.chat.audit.show',$c) }}">{{ $c->title }}</a></td><td class="p-3">{{ $c->messages_count }}</td><td class="p-3">{{ optional($c->last_message_at)->toDayDateTimeString() }}</td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $conversations->links() }}</div></x-layout.app-shell></x-layout.app>
